apply plugin: 'java'

tasks.withType(Compile) {
    options.encoding = "UTF-8"
}
repositories {
    mavenCentral()
}

dependencies {
    testCompile('junit:junit:4.4')
}
jar {
    manifest {
        archiveName = 'CarParking.jar'
    }
}